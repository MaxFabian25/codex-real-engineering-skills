interface:
  display_name: "To Issues"
  short_description: "Break plans into issue slices"
  default_prompt: "Use $to-issues to convert this plan into implementation issues."
policy:
  allow_implicit_invocation: true