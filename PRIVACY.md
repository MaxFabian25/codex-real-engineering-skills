# Privacy

This plugin does not run a service, collect telemetry, transmit data, or require credentials.

The repository contains static Codex skill instructions and helper templates. When installed, Codex reads the local plugin files from its configured plugin cache. Any data access performed while using Codex is governed by the user's Codex runtime, enabled tools, and workspace configuration, not by this repository.
